import { SafeBgUrlPipe } from './safe-bg-url.pipe';

describe('SafeBgUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeBgUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
